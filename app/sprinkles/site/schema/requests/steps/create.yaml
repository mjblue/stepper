---
steps:
  validators:
    required:
      label: "&STEPS"
      message: VALIDATE.REQUIRED
    length:
      label: "&STEPS"
      min: 1
      max: 255
      message: VALIDATE.LENGTH_RANGE
  transformations:
  - trim
date:
  validators:
    required:
      label: "&DATE"
      message: VALIDATE.REQUIRED
    length:
      label: "&DATE"
      min: 1
      max: 255
      message: VALIDATE.LENGTH_RANGE
user_id:
  validators:
    required:
      label: "&USER_ID"
      message: VALIDATE.REQUIRED
    length:
      label: "&USER_ID"
      min: 1
      max: 255
      message: VALIDATE.LENGTH_RANGE
description: 
